from starlette.middleware.cors import CORSMiddleware from pydantic import AnyHttpUrl class ApiWrapper(): def __init__(self, app, origins): self.app = app self.origins = origins for origin in self.origins: cors_allowed_origins.append(origin) if isinstance(self.origins, (list, tuple)): self.app.add_middleware(CORSMiddleware, allow_origins=self.origins, allow_credentials=True, allow_methods=['OPTIONS', 'GET', 'POST', 'PUT', 'PATCH', 'DELETE'], allow_headers=['*'])